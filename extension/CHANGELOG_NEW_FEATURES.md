# Нові функції аналізу коду

## Додано 5 нових типів аналізу:

### 1. 📤 Невикористані експорти

- Знаходить всі `export` які не імпортуються в інших файлах
- Допомагає зменшити розмір бандлу
- Показує файл де знаходиться експорт

### 2. ⚛️ Невикористані React компоненти

- Аналізує компоненти (функції/класи що починаються з великої літери)
- Перевіряє чи використовуються в JSX (`<ComponentName />`)
- Показує файл де знаходиться компонент

### 3. 🪝 Невикористані хуки

- Знаходить кастомні хуки (функції що починаються з `use`)
- Перевіряє чи викликаються в коді
- Показує файл де знаходиться хук

### 4. 🔷 Невикористані enum / interface / type

- Аналізує TypeScript типи, інтерфейси та енуми
- Перевіряє чи використовуються в типізації
- **Враховує залежності між типами** (якщо `type User = TUser | TAdmin`, то `TUser` і `TAdmin` вважаються використаними)
- Підтримує union types (`Type1 | Type2`), intersection types (`Type1 & Type2`)
- Розпізнає використання в дженериках (`Array<Type>`, `Promise<Type>`)
- Показує тип (enum/interface/type) та файл

### 5. 🌐 Невикористані API ендпоінти

- Знаходить API роути (Next.js `/api/*`)
- Перевіряє чи викликаються через `fetch()` або `axios`
- Показує шлях до ендпоінту та файл

## Розташування блоків

Нові блоки відображаються після блоку "Невикористані зображення" у такому порядку:

1. Невикористані експорти
2. Невикористані React компоненти
3. Невикористані хуки
4. Невикористані enum/interface/type
5. Невикористані API ендпоінти

## Адаптивність до різних структур проектів

Аналізатори працюють з будь-якою структурою проекту:

- Next.js (App Router / Pages Router)
- React (CRA / Vite)
- Vue / Nuxt
- Angular
- Будь-які інші JavaScript/TypeScript проекти

## Технічні деталі

### Файли що були змінені:

- `extension/components/analyzers.js` - додано 5 нових функцій аналізу
- `extension/components/zip-handler.js` - інтеграція нових аналізаторів
- `extension/popup-main.js` - рендеринг нових блоків результатів

### Нові функції в `window.Analyzers`:

- `analyzeUnusedExports(jsFiles)`
- `analyzeUnusedComponents(jsFiles)`
- `analyzeUnusedHooks(jsFiles)`
- `analyzeUnusedEnumsInterfaces(jsFiles)`
- `analyzeUnusedAPIEndpoints(jsFiles)`

### Статистика

Додано нові поля в `stats`:

- `totalExports`
- `totalComponents`
- `totalHooks`
- `totalEnumsInterfaces`
- `totalAPIEndpoints`

## Покращення алгоритму аналізу типів

### Проблема

Раніше типи, які використовувалися в union/intersection типах, помилково визначалися як невикористані:

```typescript
export type TUser = { id: string; name: string };
export type TAdmin = { id: string; role: string };
export type User = TUser | TAdmin; // TUser і TAdmin вважалися невикористаними
```

### Рішення

Тепер аналізатор:

1. Зберігає повні визначення всіх типів
2. Перевіряє залежності між типами
3. Якщо тип використовується, всі типи в його визначенні також позначаються як використані
4. Виконує ітеративну перевірку для ланцюжків залежностей (до 10 рівнів)

### Приклад роботи

```typescript
// Файл types.ts
export type TUser = { id: string; };
export type TAdmin = { id: string; };
export type TOwner = { id: string; };
export type User = TUser | TAdmin | TOwner;

// Файл app.ts
import { User } from './types';
const user: User = ...;
```

**Результат:** Тільки `User` вважається використаним, а `TUser`, `TAdmin`, `TOwner` також позначаються як використані через залежність.
