# UI Renderer Modules

Модулі для рендерингу різних секцій результатів аналізу проекту.

## Структура

Головний файл `ui-renderer.js` містить тільки:
- `renderResultsHTML()` - головна функція для рендерингу загального інтерфейсу
- `renderDetailedBlocks()` - оркестратор, що викликає всі renderer модулі

Всі специфічні render-функції винесені в окремі модулі для кращої підтримки та читабельності коду.

## Модулі

### Утиліти
- **renderer-utils.js** - Допоміжні функції:
  - `getWordForm()` - правильна форма слова (1 файл / 2 файли / 5 файлів)
  - `getCommonLibraries()` - база даних відомих бібліотек з описами

### Візуалізація проекту
- **renderer-project-styles.js** - Відображення CSS фреймворків (Tailwind, Bootstrap, MUI, Styled Components, CSS Modules)
- **renderer-component-tree.js** - Файлове дерево проекту з іконками та розмірами файлів
- **renderer-component-dependencies.js** - Treemap візуалізація залежностей компонентів

### Аналіз файлів
- **renderer-file-types.js** - Статистика типів файлів у проекті
- **renderer-dependencies.js** - Використовувані бібліотеки та залежності з описами
- **renderer-dependency-analysis.js** - Детальний аналіз залежностей (циклічні залежності, god files, hub files)

### Аналіз безпеки та архітектури
- **renderer-auth-analysis.js** - Способи авторизації (JWT, OAuth, Firebase, NextAuth, Auth0)
- **renderer-storage-analysis.js** - Використані сховища (localStorage, Redux, Zustand, cookies)
- **renderer-code-health.js** - Загальне здоров'я коду (дублікати, циклічні залежності, велі файли)

### Аналіз невикористаного коду
- **renderer-unused-css.js** - Невикористані CSS класи
- **renderer-unused-functions.js** - Невикористані JavaScript функції
- **renderer-unused-variables.js** - Невикористані змінні
- **renderer-unused-images.js** - Невикористані зображення
- **renderer-unused-exports.js** - Невикористані експорти
- **renderer-unused-components.js** - Невикористані React компоненти
- **renderer-unused-hooks.js** - Невикористані React hooks
- **renderer-unused-enums-interfaces.js** - Невикористані TypeScript типи, enum, interfaces
- **renderer-unused-api-endpoints.js** - Невикористані API endpoints

### Інші аналізи
- **renderer-duplicate-functions.js** - Дублікати функцій з відсотком схожості
- **renderer-api-routes.js** - Знайдені API endpoints (Next.js, Express, fetch, axios)
- **renderer-pages.js** - Сторінки та роути додатку
- **renderer-typescript-types.js** - TypeScript типи та interfaces
- **renderer-recommendations.js** - Рекомендації для оптимізації проекту

## Як працює

1. Всі модулі доповнюють глобальний об'єкт `window.UIRenderer`
2. Кожен модуль експортує функцію з назвою `render{Name}`, наприклад:
   ```javascript
   window.UIRenderer.renderAPIRoutes = function (result) { ... }
   ```
3. Головний файл `ui-renderer.js` викликає всі ці функції в потрібному порядку через `renderDetailedBlocks()`
4. Модулі завантажуються в popup.html **перед** ui-renderer.js

## Порядок завантаження в popup.html

```html
<!-- Утиліти завантажуються першими -->
<script src="components/ui-renderer/renderer-utils.js"></script>

<!-- Далі всі інші renderer модулі -->
<script src="components/ui-renderer/renderer-project-styles.js"></script>
<script src="components/ui-renderer/renderer-component-tree.js"></script>
<!-- ... інші модулі ... -->

<!-- Головний файл завантажується останнім -->
<script src="components/ui-renderer.js"></script>
```

## Додавання нового модуля

1. Створіть новий файл `renderer-my-feature.js`
2. Експортуйте функцію:
   ```javascript
   window.UIRenderer = window.UIRenderer || {};

   window.UIRenderer.renderMyFeature = function (result) {
     if (!result.myData) return '';

     let html = `
       <div class="analysis-block">
         <div class="block-header">
           <h3>🎯 Назва блоку</h3>
           <p>Опис</p>
         </div>
         <!-- Вміст -->
       </div>
     `;
     return html;
   };
   ```
3. Додайте скрипт у `popup.html` перед `ui-renderer.js`
4. Додайте виклик у `renderDetailedBlocks()` в `ui-renderer.js`:
   ```javascript
   this.renderMyFeature(result) +
   ```

## Використання допоміжних функцій

```javascript
// Правильна форма слова
const text = `${count} ${this.getWordForm(count, ["файл", "файли", "файлів"])}`;

// База даних бібліотек
const libraries = this.getCommonLibraries();
const description = libraries['react'] || 'Опис відсутній';
```

## Переваги модульної структури

- ✅ Кожен модуль відповідає за одну секцію
- ✅ Легко знайти та відредагувати потрібний блок
- ✅ Зменшення розміру ui-renderer.js з 2400+ рядків до ~200 рядків
- ✅ Можливість паралельної роботи над різними модулями
- ✅ Простіше тестування та відлагодження
- ✅ Кращий контроль версій (менше конфліктів при merge)
