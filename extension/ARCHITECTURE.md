# Архітектура проекту

## 📊 Діаграма залежностей

```
┌─────────────────────────────────────────────────────────────┐
│                        popup.html                            │
│  (завантажує всі скрипти в правильному порядку)             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    lib/jszip.min.js                          │
│              (зовнішня бібліотека для ZIP)                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  components/utils.js                         │
│         window.Utils { escapeHTML, calculateSimilarity }     │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                ▼                           ▼
┌───────────────────────────┐   ┌───────────────────────────┐
│  components/analyzers.js  │   │ components/api-analyzer.js│
│  window.Analyzers {       │   │  window.APIAnalyzer {     │
│    analyzeCSSClasses,     │   │    analyzeAPIRoutes,      │
│    analyzeFunctions,      │   │    extractNextJSPath      │
│    analyzeVariables,      │   │  }                        │
│    analyzeImages,         │   └───────────────────────────┘
│    findDuplicateFunctions │                │
│  }                        │                │
└───────────────────────────┘                │
                │                            │
                └────────────┬───────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│              components/zip-handler.js                       │
│  window.ZipHandler {                                         │
│    extractZipFiles,                                          │
│    analyzeZipProject  ← викликає всі аналізатори            │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│         components/ui-renderer-details.js                    │
│  window.UIRendererDetails { commonLibs }                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              components/ui-renderer.js                       │
│  window.UIRenderer {                                         │
│    renderResultsHTML,                                        │
│    renderDetailedBlocks                                      │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│           lib/zip-analyzer.js (legacy)                       │
│  Старий код для сумісності, функції перенесені в компоненти │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    popup-main.js                             │
│  Головний контролер:                                         │
│  - Event listeners                                           │
│  - Виклик window.ZipHandler.analyzeZipProject()             │
│  - Виклик window.UIRenderer.renderResultsHTML()             │
│  - Містить велику функцію renderDetailedBlocks()            │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Потік даних

```
1. Користувач вибирає ZIP файл
         │
         ▼
2. popup-main.js читає файл як ArrayBuffer
         │
         ▼
3. window.ZipHandler.analyzeZipProject(dataView)
         │
         ├─→ extractZipFiles() → масив файлів
         │
         ├─→ window.Analyzers.analyzeCSSClasses()
         ├─→ window.Analyzers.analyzeFunctions()
         ├─→ window.Analyzers.analyzeVariables()
         ├─→ window.Analyzers.analyzeImages()
         ├─→ window.Analyzers.findDuplicateFunctions()
         ├─→ window.APIAnalyzer.analyzeAPIRoutes()
         ├─→ window.Analyzers.analyzeFileTypes()
         ├─→ window.Analyzers.analyzePages()
         └─→ window.Analyzers.analyzeTypeScriptTypes()
         │
         ▼
4. Повертає об'єкт result з усіма даними
         │
         ▼
5. window.UIRenderer.renderResultsHTML(result)
         │
         ├─→ Рендерить основні метрики
         ├─→ Рендерить архітектуру
         └─→ Викликає renderDetailedBlocks(result)
         │
         ▼
6. HTML вставляється в DOM
         │
         ▼
7. Користувач бачить результати
```

## 🏗️ Структура папок

```
extension/
├── components/              # Модульні компоненти
│   ├── utils.js            # Базові утиліти
│   ├── analyzers.js        # Аналізатори коду
│   ├── api-analyzer.js     # Аналіз API
│   ├── zip-handler.js      # Робота з ZIP
│   ├── ui-renderer.js      # Рендеринг UI
│   ├── ui-renderer-details.js  # Деталі UI
│   └── README.md           # Документація компонентів
│
├── lib/                    # Бібліотеки
│   ├── jszip.min.js       # ZIP бібліотека
│   └── zip-analyzer.js    # Legacy код
│
├── icons/                  # Іконки екстеншена
│
├── popup.html             # HTML інтерфейс
├── popup-main.js          # Головний контролер
├── popup-main-new.js      # Новий контролер (опціонально)
├── styles.css             # Стилі
├── manifest.json          # Конфігурація екстеншена
│
└── Документація:
    ├── COMPONENT_STRUCTURE.md   # Структура компонентів
    ├── MIGRATION_GUIDE.md       # Інструкція з міграції
    ├── QUICK_REFERENCE.md       # Швидкий довідник
    └── ARCHITECTURE.md          # Цей файл
```

## 🎯 Принципи дизайну

### 1. Модульність

Кожен компонент відповідає за одну область функціональності:

- `utils.js` - загальні утиліти
- `analyzers.js` - аналіз коду
- `api-analyzer.js` - аналіз API
- `zip-handler.js` - робота з ZIP
- `ui-renderer.js` - відображення

### 2. Глобальні namespace

Замість ES6 модулів використовуємо `window.*`:

```javascript
window.Utils = { ... }
window.Analyzers = { ... }
window.APIAnalyzer = { ... }
```

### 3. Послідовне завантаження

Файли завантажуються через `<script>` теги в правильному порядку.

### 4. Зворотна сумісність

Старий код (`lib/zip-analyzer.js`) залишений для сумісності.

### 5. Без збірки

Код працює напряму в браузері без webpack/rollup.

## 🔧 Розширення

### Додати новий аналізатор

1. Додай функцію в `components/analyzers.js`:

```javascript
window.Analyzers.myAnalyzer = function (files) {
  // логіка
  return result;
};
```

2. Викликай в `components/zip-handler.js`:

```javascript
const myResult = window.Analyzers.myAnalyzer(files);
```

3. Додай в результат:

```javascript
resolve({
  ...
  myResult: myResult
});
```

### Додати новий UI блок

1. Додай функцію в `components/ui-renderer.js`:

```javascript
window.UIRenderer.renderMyBlock = function (data) {
  return "<div>" + data + "</div>";
};
```

2. Викликай в `renderDetailedBlocks()`:

```javascript
html += window.UIRenderer.renderMyBlock(result.myData);
```

## 📈 Метрики

- **Загальний розмір коду**: ~3000 рядків
- **Кількість компонентів**: 6
- **Кількість аналізаторів**: 8
- **Підтримувані фреймворки**: React, Next.js, Vue, Angular
- **Типи файлів для аналізу**: JS, JSX, TS, TSX, CSS, SCSS, SASS, LESS

## 🎓 Навчальні ресурси

- `COMPONENT_STRUCTURE.md` - детальний опис структури
- `MIGRATION_GUIDE.md` - як мігрувати зі старого коду
- `QUICK_REFERENCE.md` - швидкий довідник API
- `components/README.md` - документація компонентів
