# DevHelper Code Analysis - Browser Extension

🔍 Браузерний плагін для аналізу невикористаного коду в веб-проектах

## Можливості

- ✅ **ZIP Upload** - Завантаження та аналіз цілого проекту
- ✅ **Current Page Scan** - Сканування CSS на поточній сторінці
- ✅ **Виявлення невикористаних CSS класів**
- ✅ **Виявлення невикористаних JavaScript функцій**
- ✅ **Автоматична фільтрація Tailwind utility класів**
- ✅ **Експорт результатів** (JSON, TXT)
- ✅ **Детальна статистика** аналізу

## Встановлення

### Chrome / Edge / Brave

1. Відкрийте `chrome://extensions/` (або `edge://extensions/`)
2. Увімкніть "Режим розробника" (Developer mode)
3. Натисніть "Завантажити розпаковане розширення" (Load unpacked)
4. Виберіть папку `extension`

### Firefox

1. Відкрийте `about:debugging#/runtime/this-firefox`
2. Натисніть "Завантажити тимчасовий додаток" (Load Temporary Add-on)
3. Виберіть файл `extension/manifest.json`

## Використання

### 📦 ZIP Upload

1. Натисніть на іконку плагіна в браузері
2. Перейдіть на вкладку "ZIP Upload"
3. Натисніть "Вибрати ZIP файл"
4. Виберіть архів вашого проекту
5. Дочекайтеся завершення аналізу
6. Перегляньте результати та експортуйте звіт

**Що аналізується:**

- Всі CSS файли (крім node_modules, .next, dist)
- Всі JS/TS/JSX/TSX файли
- Автоматична фільтрація Tailwind класів
- Пошук невикористаних функцій та експортів

### 🌐 Current Page Scan

1. Відкрийте будь-яку веб-сторінку
2. Натисніть на іконку плагіна
3. Перейдіть на вкладку "Current Page"
4. Натисніть "Почати сканування"
5. Перегляньте невикористані CSS класи на сторінці

**Обмеження:**

- Аналізує тільки поточну сторінку
- Класи з інших сторінок можуть бути позначені як невикористані
- Для повного аналізу використайте ZIP upload

## Експорт результатів

### JSON формат

```json
{
  "timestamp": "2024-01-01T12:00:00.000Z",
  "stats": {
    "totalCSSClasses": 150,
    "totalFunctions": 45,
    "cssFilesAnalyzed": 8,
    "jsFilesAnalyzed": 23
  },
  "unusedCSS": [{ "name": "old-class", "location": "styles.css" }],
  "unusedFunctions": [{ "name": "oldFunction()" }]
}
```

### TXT формат

```
=== ЗВІТ ПРО НЕВИКОРИСТАНИЙ КОД ===
Дата: 01.01.2024, 12:00:00

📊 СТАТИСТИКА:
CSS класів: 150
Функцій: 45
CSS файлів: 8
JS файлів: 23

🎨 НЕВИКОРИСТАНІ CSS КЛАСИ (12):
  - old-class (styles.css)
  - unused-button (components.css)
  ...
```

## Технічні деталі

### Структура проекту

```
extension/
├── manifest.json       # Конфігурація плагіна
├── popup.html         # HTML інтерфейсу
├── popup.js           # Логіка плагіна
├── styles.css         # Стилі інтерфейсу
├── background.js      # Service worker
├── icons/             # Іконки плагіна
└── README.md          # Документація
```

### Алгоритм аналізу

**CSS класи:**

1. Витягування всіх класів з CSS файлів
2. Пошук використання в JS/JSX/TS/TSX файлах
3. Фільтрація Tailwind utility класів
4. Формування списку невикористаних

**JavaScript функції:**

1. Пошук оголошень функцій (function, const, export)
2. Підрахунок використань кожної функції
3. Позначення функцій з 1 використанням як невикористаних

### Фільтрація Tailwind

Автоматично пропускаються:

- Префікси: `hover:`, `focus:`, `active:`, `disabled:`, `sm:`, `md:`, `lg:`, `xl:`, `2xl:`
- Utility класи: `flex`, `grid`, `block`, `inline`, `hidden`
- Класи з префіксами: `text-`, `bg-`, `p-`, `m-`, `w-`, `h-`

## Рекомендації

### Для кращих результатів:

1. **Використайте ZIP upload** для повного аналізу проекту
2. **Налаштуйте PurgeCSS** для автоматичного очищення CSS
3. **Використайте ESLint** з правилом `no-unused-vars`
4. **Налаштуйте tree-shaking** у webpack/vite
5. **Використайте webpack-bundle-analyzer** для аналізу bundle

### Інструменти для глибшого аналізу:

- **webpack-bundle-analyzer** - аналіз розміру bundle
- **ESLint** - статичний аналіз JavaScript
- **PurgeCSS** - автоматичне очищення CSS
- **Chrome DevTools Coverage** - runtime аналіз покриття коду

## Обмеження

- Аналіз JavaScript функцій є базовим (для повного аналізу використайте ESLint)
- Динамічні класи (template strings) можуть бути пропущені
- CORS обмеження для зовнішніх stylesheets
- Складні селектори можуть викликати помилки парсингу

## Підтримка

Якщо виникли проблеми:

1. Перевірте консоль браузера на помилки
2. Переконайтеся, що ZIP архів не пошкоджений
3. Перевірте, що сторінка повністю завантажена перед скануванням

## Ліцензія

MIT License

## Автор

DevHelper Team
