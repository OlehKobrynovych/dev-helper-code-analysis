# DevHelper Script Modules

Модульна структура скрипта DevHelper для кращої читабельності та підтримки.

## 📁 Структура модулів

```
modules/
├── core.js                  # Базова логіка перехоплення помилок
├── errors-tab.js            # Відображення списку помилок
├── performance-tab.js       # Моніторинг продуктивності (FPS, Memory, Load Time)
├── code-analysis-tab.js     # Аналіз невикористаного коду
├── testing-tab.js           # Тестування помилок
├── modal.js                 # UI модального вікна та табів
└── README.md               # Ця документація
```

## 🧩 Опис модулів

### 1. **core.js** - Ядро системи

**Відповідальність:**

- Перехоплення `console.error` та `console.warn`
- Обробка глобальних помилок (`window.error`, `unhandledrejection`)
- Витягування інформації про файл та рядок з stack trace
- AI аналіз помилок
- Генерація та відправка звітів

**Експортує:**

- `createDevHelperCore(config, baseUrl)` - створює core instance

**Повертає:**

```javascript
{
  errors: Array,
  getErrors: () => Array,
  clearErrors: () => void,
  sendReport: () => void,
  downloadReport: () => void,
  analyzeWithAI: () => Promise
}
```

---

### 2. **errors-tab.js** - Таб з помилками

**Відповідальність:**

- Рендеринг списку помилок
- Відображення AI аналізу та severity badges
- Показ рекомендацій
- Інформація про файл та рядок коду
- Stack trace

**Експортує:**

- `renderErrorsTab(errors)` - повертає HTML string

---

### 3. **performance-tab.js** - Таб продуктивності

**Відповідальність:**

- Моніторинг FPS (Frames Per Second)
- Відстеження використання пам'яті (JavaScript Heap)
- Метрики завантаження (Load Time, FCP, DOM Content Loaded)
- Виявлення проблем продуктивності
- Рекомендації з оптимізації

**Експортує:**

- `renderPerformanceTab(modal)` - повертає HTML string
- `startPerformanceMonitoring(modal)` - запускає моніторинг

**Метрики:**

- 🎮 FPS: 60 = ідеально, 30-55 = прийнятно, <30 = погано
- 💾 Memory: <50% = добре, 50-75% = увага, >75% = критично
- ⚡ Load Time: <1s = відмінно, 1-3s = добре, >3s = погано

---

### 4. **code-analysis-tab.js** - Таб аналізу коду

**Відповідальність:**

- Сканування проекту на невикористаний код
- Виявлення невикористаних CSS класів
- Виявлення невикористаних JavaScript функцій
- Статистика по файлах
- Рекомендації з очищення

**Експортує:**

- `renderCodeAnalysisTab(baseUrl, config)` - повертає HTML string

**API:**

- `POST /api/devhelper/analyze-project` - сканує проект

---

### 5. **testing-tab.js** - Таб тестування

**Відповідальність:**

- 8 типів тестових помилок
- Генерація помилок для демонстрації
- Підказки для користувачів

**Експортує:**

- `renderTestingTab()` - повертає HTML string

**Тестові функції:**

- ❌ Console Error
- ⚠️ Console Warning
- ⚡ Runtime Error
- ℹ️ Promise Rejection
- ❌ Undefined Error
- ❌ Type Error
- ⚠️ Network Error
- ℹ️ Multiple Errors

---

### 6. **modal.js** - UI модального вікна

**Відповідальність:**

- Створення модального вікна
- Управління табами
- Floating button з badge
- Header, Footer, Navigation

**Експортує:**

- `createModal(core, baseUrl, config)` - створює modal instance
- `createFloatingButton(modal)` - створює floating button

**Повертає:**

```javascript
{
  show: () => void,
  updateBadge: (count) => void
}
```

---

## 🔄 Як працює інтеграція

### route.ts (головний файл)

```typescript
1. Читає всі модулі з файлової системи
2. Видаляє ES6 import/export (для браузера)
3. Об'єднує в один IIFE скрипт
4. Додає головну логіку ініціалізації
5. Експортує як JavaScript response
```

### Порядок виконання:

```
1. core.js         → Перехоплює помилки
2. *-tab.js        → Готові до рендерингу
3. modal.js        → Створює UI
4. MAIN            → Ініціалізує все разом
```

---

## 🛠️ Як додати новий таб

1. **Створіть новий файл** `my-tab.js`:

```javascript
export function renderMyTab() {
  return "<div>My content</div>";
}
```

2. **Додайте в route.ts**:

```typescript
const myTabModule = readFileSync(join(modulesDir, "my-tab.js"), "utf-8");
// ...
${cleanModule(myTabModule)}
```

3. **Додайте кнопку в modal.js**:

```javascript
const myTab = createTabButton("🎯 My Tab", "mytab");
tabButtons.appendChild(myTab);
```

4. **Додайте рендеринг в modal.js**:

```javascript
else if (currentTab === 'mytab') {
  body.innerHTML = renderMyTab();
}
```

---

## 📝 Переваги модульної структури

✅ **Читабельність** - кожен модуль відповідає за одну функцію  
✅ **Підтримка** - легко знайти та виправити код  
✅ **Тестування** - можна тестувати модулі окремо  
✅ **Розширюваність** - легко додавати нові таби  
✅ **Повторне використання** - модулі можна використовувати в інших проектах

---

## 🔍 Debugging

Якщо щось не працює:

1. **Перевірте консоль браузера** - чи завантажився скрипт
2. **Перевірте Network tab** - чи є помилки в `/api/devhelper/script`
3. **Перевірте чи всі модулі читаються** - додайте console.log в route.ts
4. **Перевірте синтаксис** - ES6 import/export мають бути видалені

---

## 📦 Розмір модулів

| Модуль               | Рядків   | Розмір    |
| -------------------- | -------- | --------- |
| core.js              | ~220     | ~7KB      |
| errors-tab.js        | ~100     | ~4KB      |
| performance-tab.js   | ~250     | ~10KB     |
| code-analysis-tab.js | ~150     | ~6KB      |
| testing-tab.js       | ~100     | ~4KB      |
| modal.js             | ~150     | ~5KB      |
| **Всього**           | **~970** | **~36KB** |

_Після мініфікації: ~18KB_
_Після gzip: ~6KB_
